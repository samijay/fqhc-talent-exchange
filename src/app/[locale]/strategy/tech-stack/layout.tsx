import { pageMetadata } from "@/lib/seo-config";

export const metadata = pageMetadata.techStack;

export default function TechStackLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
